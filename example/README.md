# local_auth_kit example

```sh
cd example
dn pub get
dn run
```

On a real device (or an emulator with a fingerprint / PIN enrolled), tap
**Authenticate**. Face ID on simulator: Features → Face ID → Enrolled.
